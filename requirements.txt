# Pinned to the versions the smoke test passes against. Loose pins let the
# Streamlit Cloud builder resolve something newer than the app was written for,
# and that failure surfaces as a blank deployed app rather than a local error.
#
# Every entry is verified by dashboard-mockup/test_clean_build.sh, which installs
# ONLY this file into an empty virtualenv and runs the suite. Two imports were
# missing when that check was first run: PyYAML, which auth.py needs and which
# would have crashed the app on the login screen, and openpyxl, which pandas
# needs to read an uploaded .xlsx on the target-lead import.
streamlit==1.63.0
altair==5.5.0
pandas==2.2.3
numpy==2.1.3
PyYAML==6.0.2
openpyxl==3.1.5
